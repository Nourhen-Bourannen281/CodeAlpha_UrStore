import React from 'react';

const ProductFilter = ({ currentFilters, darkMode }) => {
  return (
    <div className={`product-filter ${darkMode ? 'dark' : ''}`}>
      {/* Interface de filtrage ici */}
    </div>
  );
};

export default ProductFilter;